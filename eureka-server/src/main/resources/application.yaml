server:
  port: 8761

spring:
  application:
    name: eureka-server

# A standalone registry: it neither registers itself nor pulls the registry,
# because it is the registry. Works identically locally and in Docker.
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
